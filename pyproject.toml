[tool.ruff]
target-version = "py310"
line-length = 100
extend-exclude = ["bin", "dist", "out", "cli"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
line-ending = "lf"